@model Kurs

<p><b>@Model.Baslik</b> adli kursu silmek istediynize eminsiz?</p>

    <form asp-action="Delete" method="post">
        <input type="hidden" name="id" value="@Model.KursId">
        <button type="submit" class="btn btn-danger">Beli</button>
        <a asp-action="Index" class="btn btn-warning">Xeyr</a>
    </form>